/**
 * Placeholder. The real screens (host setup checklist, deck select, round) arrive
 * once auth and playback exist — see docs/06-iteration-1-plan.md.
 *
 * This exists only to prove the toolchain boots.
 */
export function App() {
  return (
    <main>
      <h1>Song Timeline</h1>
      <p>Scaffold only — no game UI yet.</p>
    </main>
  );
}
